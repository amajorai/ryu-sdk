[package]
name = "ryu-kernel-contracts"
version = "0.2.8"
edition = "2021"
description = "Ryu kernel contracts — the single, pure-data definition of the manifest.json manifest model (types + validation) shared by apps/core and the Ryu SDK. No I/O, no runtime deps; serde/schemars only."
license = "Apache-2.0"
repository = "https://github.com/amajorai/ryu-sdk"

[lib]
name = "ryu_kernel_contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "1"
semver = "1"
url = "2"
