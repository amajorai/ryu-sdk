# The importable surface is GENERATED by uniffi-bindgen-cs (see README + the
# justfile `gen-csharp` recipe), not committed. Only the packaging shell
# (ryu_sdk.csproj), the smoke (Program.cs), and this README are tracked — matching
# the Python binding's committed-vs-generated split.
/ryu_sdk.cs
*.dll
*.so
*.dylib
bin/
obj/
