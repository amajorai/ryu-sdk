{
  "name": "resolvedesk-support-widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build-manifest": "bun run src/app.ts",
    "pack": "ryu pack .",
    "check-types": "bun run --cwd ../../packages/sdk build && tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@ryuhq/sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
